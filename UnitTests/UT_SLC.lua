UT.TestCase("SLC", 
function()
      
  -- SLC.RelinkCargo(cargo)
  
  -- SLC.GenerateName(n)
  
  -- SLC.InAir(obj)
  
  -- SLC.IsCargoValid(cargo)
  
  -- SLC.GetNearbyCargo(groupTransport)
  
  -- SLC.GetNearbyInfantryGroups(groupTransport)
  
  -- SLC.IsAssemblyValid(assembler, crates)
  
  -- SLC.GetAssemblers(crates)
  
  -- SLC.SpawnGroup(g, pilotName, infcomp)
  
  -- SLC.SpawnCrate(g, pilotName, comp)
  
  -- SLC.SpawnSlingLoadableCargo(rawtype, comp, pos)
  
  -- SLC.Unpack(transportGroup, pilotname)
  
  -- SLC.UnloadTroops(g, p)
  
  -- SLC.LoadTroops(g, p)
  
  -- SLC.LoadUnload(g, pilot)
      
  -- SLC.SlingLoadableCargoInZone(cargo_name, zone)
      
      
end)